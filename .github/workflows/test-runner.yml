name: Test GitHub Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Runner info
        run: |
          echo "GitHub Runner is working!"
          uname -a
          python3 --version || true
